services:
  prompt-router:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-router
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
